# Playwright is dev-only E2E tooling. Never let `npm ci` (the Docker build runs
# it) pull the ~150 MB browser bundle — that download belongs to an explicit
# `npx playwright install chromium`, run once by whoever runs the e2e suite.
playwright_skip_browser_download=1
