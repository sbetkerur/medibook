{
  "scripts": {
    "deploy:backend": "railway up --service backend --detach",
    "deploy:frontend": "railway up --service frontend --detach",
    "deploy": "npm run deploy:backend && npm run deploy:frontend"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1",
    "puppeteer": "^24.40.0",
    "puppeteer-core": "^24.39.1"
  }
}
